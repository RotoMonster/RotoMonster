@model RotoMonster.Core.MonsterBotPlayer

@if (Model != null)
{
    @foreach (var c in Model.MonsterBotPlayerComments)
    {
        <span data-bs-toggle="tooltip" data-bs-placement="top" title="@c.Text">
            <i class="@c.Icon @c.Color"></i>
        </span>
    }
}
