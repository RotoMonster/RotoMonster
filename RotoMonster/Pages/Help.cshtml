@page
@using RotoMonster.Core.PartialViewModels
@model RotoMonster.Pages.HelpModel
@{
}

<div class="container">

@*    <h2>Monster Bar</h2>
    <p>There are two types of Monster Bars, one showing a per-game value and one showing a total value.  By default, the per-game one is shown in most places.</p>

    <partial name="_DisplayMonsterBarPlayer" model="Model.DisplayMonsterBarPlayer" />
*@
    <h2>Helpers</h2>
    @foreach (var helper in Model.Helpers)
    {
        <partial name="_Helper" model="@new HelperModel(){Helper=helper,PlayerType=null}" />
    }
</div>
